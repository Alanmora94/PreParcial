import { ConvertirAImagenPipe } from './convertir-aimagen.pipe';

describe('ConvertirAImagenPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertirAImagenPipe();
    expect(pipe).toBeTruthy();
  });
});
